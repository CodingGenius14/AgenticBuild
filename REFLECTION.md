The agent did a really good job of following the SPEC.md, the only thing it struggled with initially was setting up the login and authentication part of the program.
However, out of the 24 cases it checked, the AI Agent either passed or had a small warning for 20 of the cases and failed 4 of the cases. Overall, it did a very good
job of following the core stuff outlined in the SPEC.md and getting the majority of it in 1 go.

In Phase 2 one thing I had to clarify to the AI agent was that the questions in the data format section of the SPEC.md were just example templates of how I wanted 
the actual questions to be formatted. 

The AI review was very helpful, it clearly outlined the parts of the SPEC.md that it had missed in it's first iteration and clearly labeled the status of each
feature from SPEC.md giving me a good idea what it did right and wrong and I was able to verify the results of the review by actually running the code for myself.

In the future, I would try to be very specific in my SPEC.md on how I want the app to actually work. FOr example, in the error handling, when a user entered
the wrong input type I wanted the app to raise an error but then give the user another chance to answer with the correct input type, however, I never specified
this in my SPEC.md and that's why when the app raises an error it just stops the program.

This workflow of using a SPEC.md and opening a new chat to review the code against the SPEC.md is something that I will definitely implement in the future. This
approach felt a lot more strucutred compared to other projects that I have used AI tools and the output and functionallity is something that I was very happy with
based on the instructions I gave.
